#!/bin/bash -l
echo 'begin to perform post handling'

conda activate yunda-python39
# Base dependencies have installed on AMI already
# if new dependencies are required, please install with conda command,like
# conda install xxx=1.2.3
